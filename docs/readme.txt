1. Database Schema
	Hibernate will automaticly create the nessesary tables to work. You only
	need to change the appropriate configuration in webapp/WEB-INF/datasource.properties
	Most importantly set the correct hibernate.dialect.
	
	
2. TODOs
	Remove hardcoded ID names from JavaScript, provide them via function parameters 